# Tailwind for Visual Studio Code

## Introduction

This colour theme is optimized for the `Operator Mono` or `Dank Mono` fonts.
By using more _italics_ for specific code parts it looks very elegant!
